//aula de operadores 
/*resposta pergunta 1. 
a primeira afirmação de que a constante bool1
 é falsa uma vez que bool1 e bool2 não são iguais
 porém o resultado da bool2 continua sendo falsa
 já que bool2 não é igual a bool1 e bool 3 logo a 
 é uma afirmativa incorreta e b é uma afirmativa
 correta.
 já c é uma constante para verificar se o resultad
 de c são valores diferentes, resultando ser uma afirmação
 verdadeira, pois são diferentes, portanto || não
 resultaria na sentença o valor correto já que é somente usado
 em caso de todos os retornos serem falsos
 logo o resultado de d é que são respostas baseadas
 em boleanos
 já */

/*resposta pergunta 2
 O segundo problema se dá porque não existe uma soma feita
 como é escrita no código pelo meu colega, logo o que é impresso
 é apenas os dois números, lado à lado*/

/* resposta pergunta 3 a solução mais viável seria somente explicar ao meu colega que havia a necessidade de
que ele convertesse o código em string para number como no exemplo abaixo 

let primeiroNumero = prompt("Digite um numero!")
let segundoNumero = prompt("Digite outro numero!")

const soma = Number(primeiroNumero) + Number(segundoNumero)

console.log (soma) */


//Exercícios 
// 1

let qualSeuNome = prompt ("Qual é seu nome, invocador?")
let idadeMelhorAmigoImaginário = prompt ("Qual sua idade, ser imaginário?")

console.log (qualSeuNome)
console.log (idadeMelhorAmigoImaginário)


const idadeMaiorQue = prompt ("Sua idade é menor que a de seu amigo?") = false
const minhaIdade = 26
const idadeMelhorAmigo = 22

let resultado = Number(minhaIdade) === Number(idadeMelhorAmigo) //não tenho melhor amigo

console.log (resultado)

const subtrair = 26 - 22

console.log (subtrair) 

//exercício 2

let umNúmeroPar = prompt ("Olá jogador, diga um número par!")

const divisão = (Number(umNúmeroPar) / 2)

console.log (divisão)

const resto = (Number(umNúmeroPar) / 2)

console.log (resto)

//nenhuma delas dá um número quebrado com resto
//Porém após testar o número impar, ele mostra que há um número com resto

//exercício 3























